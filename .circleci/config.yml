 version: 2
 jobs:
   deploy:
     steps:
     - run: |
           if [ "$HOST_NAME" = "ubuntu" ]; then
               echo hello
           fi
 workflows:
   version: 2
   build-and-deploy:
     jobs:
       - deploy:
           filters:
             branches:
               only: main # only deploy on the main branch
